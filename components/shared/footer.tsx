import {Component, default as React} from "react";

export default class Footer extends Component {

    render() {
        return (
            <footer>
                Footer
            </footer>
        );
    }
}
